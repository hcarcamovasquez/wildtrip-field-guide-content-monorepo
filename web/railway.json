{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter=shared build && pnpm --filter=web build"
  },
  "deploy": {
    "startCommand": "cd .. && node web/dist/server/entry.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
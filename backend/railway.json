{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter=shared build && pnpm --filter=backend build"
  },
  "deploy": {
    "startCommand": "cd .. && pnpm --filter=backend start:prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
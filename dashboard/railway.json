{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter=shared build && pnpm --filter=dashboard build"
  },
  "deploy": {
    "startCommand": "cd .. && pnpm --filter=dashboard preview --host 0.0.0.0 --port ${PORT:-5173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}